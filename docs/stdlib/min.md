# `min`
Minimum of two values or arrays (element-wise for arrays).

### Typical Call Forms
```sac
min(a, b)
```

### Description
Generated for numeric types in `ScalarArith` and array extensions in `ArrayArith`.
