# `propagate`
With-loop propagation operator.

### Typical Call Form
```sac
... : propagate(...)
```

### Description
`propagate` is a with-loop operator used to carry and update values through an index space according to propagation rules.
