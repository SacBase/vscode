# `tob`
Converts to `byte` (FULLTYPES builds).

### Typical Call Forms
```sac
byte tob(<numeric> x)
byte[d:shp] tob(<numeric>[d:shp] x)
```

### Notes
Generated through numeric conversion macros when FULLTYPES support is enabled.
