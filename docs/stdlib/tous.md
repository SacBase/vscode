# `tous`
Converts to `ushort` (FULLTYPES builds).

### Typical Call Forms
```sac
ushort tous(<numeric> x)
ushort[d:shp] tous(<numeric>[d:shp] x)
```

### Notes
Available when FULLTYPES conversion macros are enabled.
