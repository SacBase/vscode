# `toub`
Converts to `ubyte` (FULLTYPES builds).

### Typical Call Forms
```sac
ubyte toub(<numeric> x)
ubyte[d:shp] toub(<numeric>[d:shp] x)
```

### Notes
Available when FULLTYPES conversion macros are enabled.
