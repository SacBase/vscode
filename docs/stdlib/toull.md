# `toull`
Converts to `ulonglong` (FULLTYPES builds).

### Typical Call Forms
```sac
ulonglong toull(<numeric> x)
ulonglong[d:shp] toull(<numeric>[d:shp] x)
```

### Notes
Available when FULLTYPES conversion macros are enabled.
