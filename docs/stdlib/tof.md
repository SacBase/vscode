# `tof`
Converts to float.

### Common Signatures
```sac
float tof(<numeric> x)
float[d:shp] tof(<numeric>[d:shp] x)
float tof(string str)
float, float tof(complex c)
```

### Description
Overloaded conversion from numeric values, arrays, strings, and complex numbers.
