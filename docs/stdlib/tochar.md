# `tochar`
Converts values to `char`.

### Common Signatures
```sac
char tochar(<a> x)
char[d:shp] tochar(<a>[d:shp] x)
```

### Description
Defined in `Char.sac` via `BUILT_IN(TOCHAR_S)` and `BUILT_IN(TOCHAR_A)`.
