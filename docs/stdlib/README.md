# StdLib Hover Docs

Add one Markdown file per StdLib function.

Naming rules:
- File name should be the symbol name with .md, for example shape.md
- Content is rendered directly in hover markdown

Examples:
- shape.md
- print.md
