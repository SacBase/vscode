# `fold`
With-loop fold operator.

### Typical Call Form
```sac
... : fold(fun, neutral)
```

### Description
`fold` combines generated values into a scalar or aggregate result using reducer `fun` and neutral element `neutral`.
