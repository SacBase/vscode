# `foldfix`
With-loop fixed-point fold operator.

### Typical Call Form
```sac
... : foldfix(fun, neutral)
```

### Description
`foldfix` is a fold variant used in fixed-point style reductions where evaluation proceeds to a stable state according to the operator semantics.
