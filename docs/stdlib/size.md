# `size`
Returns the number of elements in an array.

### Typical Call Form
```sac
size(arr)
```

### Description
`size` is commonly used as the total element count, equivalent to the product of the shape components.
