# `max`
Maximum of two values or arrays (element-wise for arrays).

### Typical Call Forms
```sac
max(a, b)
```

### Description
Generated for numeric types in `ScalarArith` and array extensions in `ArrayArith`.
