# `_add_SxS_`
Low-level **scalar addition** builtin.

### Description
This is an internal compiler function (builtin) that performs addition on two scalar values. It is typically wrapped by the standard library's `+` operator for general use.

### Parameters
*   `a`: First scalar.
*   `b`: Second scalar.
