# `_reshape_`
Changes the shape of arbitrarily shaped array `A` to the shape specified by vector `V`.

>**Constraint:** The product of the new shape must equal the product of the old shape (total element count must remain constant).

### Variants In `prf.def`
- `_reshape_VxA_`
