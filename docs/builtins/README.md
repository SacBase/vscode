# Builtin Hover Docs

Add one Markdown file per builtin function.

Naming rules:
- File name should match the builtin symbol exactly with .md
- For example: _shape_A_.md
- Content is rendered directly in hover markdown

Tip:
- Keep the first line a short title for fast scanning in hover.
