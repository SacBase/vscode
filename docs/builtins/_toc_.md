# `_toc_`
SaC does not support implicit type coercion. All conversions must be done explicitly via these primitives, which correspond to C-style casts.
This specific builtin converts a scalar value to a character.

### Variants In `prf.def`
- `_toc_S_`
