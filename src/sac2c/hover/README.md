# Hover Module

Token-based hover target model and fallback formatting.

## Files

- `types.ts`: hover target contracts.
- `index.ts`: token lookup + default markdown formatter.

Used by server hover providers plus parser hover-doc resolution pipeline.